{% extends 'base.html.twig' %}

{% block title %}Update your infos{% endblock %}

{% block content %}
<div class="container">
    <h1 class="mt-5">Update your infos</h1>

    <div class="row mt-4">
        <div class="col-md-6">
            {{ include('societe/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>

    <div class="row mt-4">
        <div class="col-md-6">
            <a href="{{ path('profile') }}" class="btn btn-secondary">Back to profile</a>
        </div>
    </div>

    <div class="row mt-4">
        <div class="col-md-6">
            {{ include('societe/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
