{% extends 'base.html.twig' %}

{% block title %}Modifier la société{% endblock %}

{% block content %}
<div class="container">
    <h1 class="mt-5">Modifier la société</h1>

    <div class="row mt-4">
        <div class="col-md-6">
            {{ include('societe/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>

    <div class="row mt-4">
        <div class="col-md-6">
            <a href="{{ path('app_societe_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>
    </div>

    <div class="row mt-4">
        <div class="col-md-6">
            {{ include('societe/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
