<form method="post" action="{{ path('app_postuler_delete', {'id': postuler.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ postuler.id) }}">
    <button class="btn">Delete</button>
</form>
