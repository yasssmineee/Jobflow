<form method="post" action="{{ path('app_post_reactions_delete', {'id': post_reaction.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post_reaction.id) }}">
    <button class="btn">Delete</button>
</form>
