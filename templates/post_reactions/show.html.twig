{% extends 'base.html.twig' %}

{% block title %}PostReactions{% endblock %}

{% block body %}
    <h1>PostReactions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post_reaction.id }}</td>
            </tr>
            <tr>
                <th>Likes</th>
                <td>{{ post_reaction.likes }}</td>
            </tr>
            <tr>
                <th>Dislike</th>
                <td>{{ post_reaction.dislike }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_post_reactions_index') }}">back to list</a>

    <a href="{{ path('app_post_reactions_edit', {'id': post_reaction.id}) }}">edit</a>

    {{ include('post_reactions/_delete_form.html.twig') }}
{% endblock %}
