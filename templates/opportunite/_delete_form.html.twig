<form method="post" action="{{ path('app_opportunite_delete', {'id': opportunite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ opportunite.id) }}">
    <button  class="btn bg-danger m-2 text-white py-2 px-4 mt-4">Delete</button>
</form>
